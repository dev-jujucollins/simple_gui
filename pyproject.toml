[project]
name = "simple-gui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pyside6>=6.10.2",
]

[dependency-groups]
dev = [
    "pyright>=1.1.408",
]
